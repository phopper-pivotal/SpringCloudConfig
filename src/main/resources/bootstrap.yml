spring:
  application:
    name: SpringCloudConfig
  cloud:
    config:
      watch:
        enabled: true
        delay: 5000
        initialDelay: 5000

#spring:
#  cloud:
#    config:
#      label: v1.2